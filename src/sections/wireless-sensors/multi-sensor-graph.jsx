import Stack from '@mui/material/Stack';
import Typography from '@mui/material/Typography';

import MainCard from 'components/MainCard';

export default function MultiSensorGraph() {
  return (
    <MainCard>
      <Stack spacing={1}>
        <Typography variant="h4">Multi Sensor Graphing</Typography>
        <Typography color="text.secondary">Page scaffold is ready for Multi Sensor Graphing content.</Typography>
      </Stack>
    </MainCard>
  );
}
